import { DecimalPlacesDirective } from './decimal-places.directive';

describe('DecimalPlacesDirective', () => {
  it('should create an instance', () => {
    const directive = new DecimalPlacesDirective();
    expect(directive).toBeTruthy();
  });
});
